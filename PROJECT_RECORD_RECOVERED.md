# 项目记录恢复

## 当前项目

- 项目名：流放之路集市辅助 / POE2 集市提醒器
- 技术栈：Electron 桌面应用
- 仓库发布配置：GitHub Releases，`y7bby7cqc2-bot/liufangzhilu-jishi-fuzhu`
- 当前版本：`0.1.0`
- 主要文件：
  - `main.js`：Electron 主进程、监控逻辑、自动更新、打开市集页面、自动点击 `Travel to Hideout`
  - `preload.js`：主进程和页面之间的安全 IPC 桥
  - `index.html`：软件界面结构
  - `app.js`：界面交互逻辑
  - `styles.css`：界面样式
  - `package.json`：运行、打包、发布配置

## 已恢复到的历史需求

1. 用户手动登录 POE2 官方市集。
2. 用户在官方市集手动设置筛选条件并搜索一次。
3. 用户把生成的搜索 URL 添加到工具里。
4. 工具按设定间隔后台检查搜索结果。
5. 有结果时弹窗/系统通知，并打开对应搜索页。
6. 当前未提交改动已经把逻辑改为：每次检查只要有结果，就打开搜索页并尝试点击第一个可见的 `Travel to Hideout`。
7. 当前未提交改动还把默认结果选择器改为 `.resultset .row[data-id], .resultset [data-id]`，并优先读取页面上的 `Showing N results` 数字，减少误报。
8. 软件已接入 `electron-updater`，可通过公开 GitHub Releases 做一键更新。

## 已找到的相关会话线索

- 早期需求：做一个 Electron 版 POE2 集市提醒器。
- 打包需求：生成 Windows exe，方便在 Windows 上测试。
- 分发需求：软件后续会给其他人使用，需要一键更新，并考虑 GitHub Release 权限问题。
- 优化需求：
  - 结果数量误报，需要改进识别逻辑。
  - 有结果后需要第一时间点击红框标记位置，即 `Travel to Hideout`。
  - 每次刷新只要有结果都要重复打开并点击。

## 售卖与防共享建议

推荐采用“本地免费查询 + 付费解锁传送功能”的轻量授权模式。

基础版：
- 免费无限查询。
- 免费赠送 1 次自动传送机会。
- 用完后仍可查询，但自动传送按钮/自动点击功能锁定。

付费版：
- 按设备授权，不建议一开始做复杂账号体系。
- 每个激活码绑定 1 台设备，有效期 30 天。
- 30 天内无限次自动传送。
- 可提供有限换机次数，例如每月 1 次。

为什么不建议一开始做账号密码：
- 账号系统需要数据库、登录接口、密码安全、找回密码、风控和客服处理。
- 对这个软件来说，真正要控制的是“自动传送功能是否可用”，不是用户资料。
- 先做激活码/设备授权，开发量和维护成本更低。

防共享策略：
- 首选设备绑定，而不是只绑定账号。
- 激活时生成设备指纹，例如系统平台、机器 ID、安装 ID 的组合哈希。
- 服务端记录激活码、设备指纹、到期时间、状态。
- 软件启动或使用自动传送前联网校验授权。
- 允许短时间离线缓存，例如 24 小时，避免服务器临时不可用影响正常用户。
- 同一个激活码如果频繁换设备，自动限制或标记异常。

最低成本实现：
- 不搭完整数据库也可以先用第三方表格/轻量后端保存激活码。
- 可选方案：Supabase、Firebase、Airtable、LeanCloud、Cloudflare Workers + D1。
- 支付可以先人工收款，手动生成激活码；等用户量起来后再接自动支付。

更现实的收费模型：
- 月卡：30 天自动传送，无限次使用。
- 季卡：90 天，单价略低。
- 永久版不建议早期卖，后续维护、页面改版、封控变化都会产生持续成本。

## 风险提醒

- Electron 客户端代码容易被反编译，不能把“是否付费”的核心判断只放在本地。
- 授权校验必须在服务端有最终记录，本地只保存缓存。
- 任何客户端防破解都只能提高成本，不能做到绝对防传播。
- 真正有效的是：设备绑定、在线校验、更新控制、异常封禁、合理价格。
